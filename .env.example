# MongoDB Configuration
MONGO_URI=mongodb://mongo:27017

# Frontend Configuration
FRONTEND_URL=https://www.minishield.tech

# WAF Configuration
# Update this with your actual server public IP
WAF_PUBLIC_IP=64.227.156.70

# DNS Database Configuration (MariaDB/MySQL)
DNS_DB_HOST=dns_db
DNS_DB_USER=pdns
DNS_DB_PASSWORD=pdns_password
DNS_DB_NAME=powerdns
DNS_DB_ROOT_PASSWORD=root_password

# JWT Secret (Change this in production!)
JWT_SECRET=super_secret_waf_key_change_me
